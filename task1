def swap_half(arr):
    
    mid = len(arr) // 2
   
    swapped_arr = arr[mid:] + arr[:mid]
    
    return swapped_arr

arr1 = [1, 2, 5, 6, 7, 8]
arr2 = [6, 7, 8, 9, 1, 2]

print(swap_half(arr1))
print(swap_half(arr2))